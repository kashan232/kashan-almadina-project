<tr class="inv-meta-row">
    <td colspan="{{ $colspan }}" class="text-left">
        Customer: <span class="party-name">{{ $customerName }}</span>
        @if(!empty($customerCnic))
            &nbsp;&nbsp;{{ $customerCnic }}
        @endif
    </td>
</tr>
